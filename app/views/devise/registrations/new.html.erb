<div class="row justify-content-center mt-5">
  <div class="col-xl-5 col-lg-6 col-md-7">
    <div class="text-center">
      <h1 class="h2">Создать аккаунт</h1>
      <p class="lead">Начните делать что угодно в одно мгновение</p>
      <hr class="mb-4">
      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <div class="form-group mb-3">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Почта", class: "form-control" %>
        </div>

        <div class="form-group mb-3">
          <%= f.text_field :nickname, placeholder: "Никнейм", class: "form-control" %>
        </div>

        <div class="form-group mb-3">
          <%= f.password_field :password, autocomplete: "new-password", placeholder: "Пароль", class: "form-control" %>
          <div class="text-start">
            <small>Ваш пароль должен содержать не меньше 8 символов</small>
          </div>
        </div>

        <%= f.submit "Создать аккаунт", class: "btn btn-lg btn-block btn-primary" %>
        <small>Нажимая 'Создать аккаунт' вы соглашаетесь на обработку ваших данных</small>
      <% end %>
    </div>
  </div>
</div>
