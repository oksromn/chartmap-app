<section class="py-5">
  <div class="container">
    <div class="row align-items-center">
      <div class="col-12 col-md-5 col-lg-6 order-md-2">
        <%= image_tag "main-section.png", class: "img-fluid mb-md-0" %>
      </div>

      <div class="col-12 col-md-7 col-lg-6 order-md-1">
        <h1 class="display-4 text-center text-md-start">
          <%= t('sections.main.welcome_html') %>
        </h1>

        <p class="lead text-center text-md-start text-muted mb-6 mb-lg-8">
          <%= t('sections.main.description') %>
        </p>

        <div class="text-center text-md-start">
          <%= link_to t('charts.buttons.create'), new_chart_path, class: "btn btn-primary shadow lift me-1" %>
          <%= link_to t('words.buttons.create'), new_word_path, class: "btn btn-primary-soft lift" %>
        </div>
      </div>
    </div>
  </div>
</section>

<div class="container py-4">
  <div class="row align-items-md-stretch">
    <% @charts.each do |chart| %>
      <div class="col-md-6">
        <div class="h-100 p-5 bg-light border rounded-3">
          <h2><%= chart.title %></h2>
          <p><%= t('charts.index.last_update', time: time_ago_in_words(chart.updated_at)) %></p>
          <p>Or, keep it light and add a border for some added definition to the boundaries of your content. Be sure to look under the hood at the source HTML here as we've adjusted the alignment and sizing of both column's content for equal-height.</p>
          <%= link_to t('charts.index.view'), chart_path(chart), class: "btn btn-primary" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
